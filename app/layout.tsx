import type { Metadata } from 'next';
import localFont from 'next/font/local';
import './globals.css';

const geistSans = localFont({
  src: './fonts/GeistVF.woff',
  variable: '--font-geist-sans',
  weight: '100 900',
});
const geistMono = localFont({
  src: './fonts/GeistMonoVF.woff',
  variable: '--font-geist-mono',
  weight: '100 900',
});

export const metadata: Metadata = {
  title: 'Aetheon Web Services',
  description:
    'Web Solutions for Every Need. From enterprise-level applications to personal portfolios, we bring your digital vision to life. Our Services App Development.Custom mobile applications for iOS and Android platforms Web Development. Responsive and dynamic websites for businesses of all sizes. Personal Websites. Stunning portfolios and personal sites to showcase your work.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
